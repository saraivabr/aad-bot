# Enhanced Architecture - Visual Overview

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          WhatsApp User                                   │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │ Message
                                 ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                     WhatsApp Web.js Interface                            │
│                         (index.js)                                       │
└────────────────────────────────┬────────────────────────────────────────┘
                                 ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                      Command Dispatcher                                  │
│                  (commandDispatcher.js)                                  │
│                 • Message buffering (5s)                                 │
│                 • Media handling                                         │
│                 • Routing                                                │
└──────────────┬─────────────────────────────────────┬────────────────────┘
               │                                     │
               ↓                                     ↓
    ┌──────────────────────┐            ┌──────────────────────┐
    │   v2.0 Engine        │            │   Legacy System      │
    │  (orchestrator)      │            │    (aiService)       │
    └──────────┬───────────┘            └──────────┬───────────┘
               │                                   │
               │         🆕 ENHANCED LAYER         │
               │         ═══════════════════       │
               ↓                                   ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                     MESSAGE HANDLER (NEW)                                │
│                  src/handlers/messageHandler.js                          │
│                                                                          │
│  ┌──────────────────────────┐    ┌────────────────────────────┐       │
│  │   Emotion Analysis       │    │    Memory Retrieval        │       │
│  │  (parallel execution)    │    │   (parallel execution)     │       │
│  │                          │    │                            │       │
│  │  ┌────────────────────┐  │    │  ┌──────────────────────┐ │       │
│  │  │ emotionHandler.js  │  │    │  │  memoryHandler.js    │ │       │
│  │  │                    │  │    │  │                      │ │       │
│  │  │ • OpenAI GPT-4o   │  │    │  │  Redis (fast) ──────┼─┼───┐   │
│  │  │ • 9 emotions      │  │    │  │  Postgres (persist) ┼─┼───┼─┐ │
│  │  │ • Intensity 0-1   │  │    │  │  File (fallback)    │ │   │ │ │
│  │  └────────────────────┘  │    │  └──────────────────────┘ │   │ │ │
│  └──────────────────────────┘    └────────────────────────────┘   │ │ │
│                                                                      │ │ │
│  Result: { emotion, context, emotionalPrefix, metadata }            │ │ │
└──────────────────────────────────────────────────────────────────┬──┘ │ │
                                                                    │    │ │
                     ┌──────────────────────────────────────────────┼────┘ │
                     │                                              │      │
                     ↓                                              ↓      ↓
         ┌───────────────────────┐                    ┌─────────────────────────┐
         │   openaiService.js    │                    │  Database Services      │
         │   (centralized)       │                    │                         │
         │                       │                    │  ┌───────────────────┐ │
         │ • Chat completions    │                    │  │ redisService.js   │ │
         │ • Embeddings          │                    │  │ • Connection pool │ │
         │ • Lazy loading        │                    │  │ • Health checks   │ │
         └───────────────────────┘                    │  └───────────────────┘ │
                                                      │  ┌───────────────────┐ │
                                                      │  │ pgService.js      │ │
                                                      │  │ • Connection pool │ │
                                                      │  │ • Transactions    │ │
                                                      │  └───────────────────┘ │
                                                      └─────────────────────────┘
                                                                    │
                                                                    ↓
                                                      ┌─────────────────────────┐
                                                      │  Infrastructure         │
                                                      │                         │
                                                      │  🐳 Docker Compose      │
                                                      │  • Redis 7 Alpine       │
                                                      │  • Postgres 16          │
                                                      │  • Redis Commander UI   │
                                                      │                         │
                                                      │  ⚙️  PM2                │
                                                      │  • Process management   │
                                                      │  • Auto-restart         │
                                                      │  • Log rotation         │
                                                      └─────────────────────────┘
                     ┌──────────────────────────────────────────┐
                     │                                          │
                     ↓                                          ↓
         ┌───────────────────────┐              ┌──────────────────────────┐
         │  AI Generation        │              │   Response Execution     │
         │  (existing)           │              │   (existing)             │
         │                       │              │                          │
         │ • Persona selection   │              │  🆕 smartDelay()        │
         │ • Context injection   │              │  • Emotion-aware timing  │
         │ • LLM generation      │              │  • Typing simulation     │
         │ • Tag processing      │              │                          │
         └───────────┬───────────┘              └────────────┬─────────────┘
                     │                                       │
                     │          Response Generated           │
                     └───────────────────┬───────────────────┘
                                        │
                                        ↓
                         ┌──────────────────────────────┐
                         │   Update Memory (NEW)        │
                         │   memoryHandler.js           │
                         │                              │
                         │ • Store conversation summary │
                         │ • Update emotion history     │
                         │ • Increment interaction count│
                         └──────────────────────────────┘
                                        │
                                        ↓
                         ┌──────────────────────────────┐
                         │   Response Delivery          │
                         │                              │
                         │ • Fragment messages          │
                         │ • Apply delays               │
                         │ • Send via WhatsApp          │
                         └──────────────────────────────┘
                                        │
                                        ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                          WhatsApp User                                   │
│                    (Receives emotionally aware,                          │
│                     memory-enhanced response)                            │
└─────────────────────────────────────────────────────────────────────────┘
```

## Data Flow Example

### Example Conversation Flow

```
User: "Estou muito frustrado com as vendas este mês" (message)
  ↓
[Command Dispatcher] Buffers and routes
  ↓
[🆕 Message Handler] Processes in parallel:
  ├─→ [Emotion Analysis]
  │     Input: "Estou muito frustrado com as vendas este mês"
  │     Output: { emotion: "frustrated", intensity: 0.8 }
  │
  └─→ [Memory Retrieval]
        1. Check Redis cache (key: "memory:{userId}")
        2. If not found → Query Postgres
        3. If not found → Return empty
        Output: "User João from São Paulo, owns a pizzaria."
  ↓
[Context Building]
  emotion: frustrated (0.8)
  context: Previous conversation about pizzaria
  emotionalPrefix: "😔 Percebo que você está frustrado — vamos resolver isso?"
  ↓
[AI Generation] with enhanced context
  System: Uses emotion + memory context
  Prompt: Includes emotional guidance + user history
  Output: Empathetic, contextual response
  ↓
[🆕 Smart Delay]
  Base: 2.5s (based on response length)
  Emotional boost: +1.2s (frustration intensity 0.8)
  Variance: +0.3s
  Total: 4.0s delay
  ↓
[Response Delivery]
  1. Show typing indicator
  2. Wait smart delay
  3. Send message: "😔 Percebo que você está frustrado... [response]"
  ↓
[🆕 Memory Update]
  Summary: "User frustrated with sales, discussed strategies"
  Emotion: { emotion: "frustrated", intensity: 0.8, timestamp: "..." }
  → Stored in Redis (24h) + Postgres (permanent)
  ↓
User receives: Emotionally aware, contextual response with natural timing
```

## Component Interaction Map

```
┌─────────────────────────────────────────────────────────────┐
│                    NEW COMPONENTS                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Handlers Layer          Services Layer      Utils Layer    │
│  ───────────────         ──────────────      ──────────     │
│                                                              │
│  emotionHandler    ←───→ openaiService       delay          │
│       │                        │                │            │
│       │                        │                └──→ Smart   │
│       │                        │                    Delays   │
│       │                        │                             │
│  memoryHandler     ←───→ redisService         regexExtractors│
│       │                   pgService               │          │
│       │                        │                  │          │
│       │                        │                  └──→ Data  │
│       │                        │                      Extract│
│       │                        │                             │
│  messageHandler    ←───→ [Orchestrates all]                 │
│       │                                                      │
│       └──────────────────────────────────────────────────┐  │
│                                                           │  │
└───────────────────────────────────────────────────────────┼──┘
                                                            │
                                    Integration Point       │
                                    ─────────────────       │
                                                            ↓
                        ┌───────────────────────────────────────┐
                        │      EXISTING COMPONENTS               │
                        ├───────────────────────────────────────┤
                        │                                        │
                        │  commandDispatcher.js                 │
                        │           │                            │
                        │           ├──→ conversationOrchestrator│
                        │           │                            │
                        │           └──→ aiService (legacy)      │
                        │                                        │
                        └────────────────────────────────────────┘
```

## Memory System Architecture

```
┌───────────────────────────────────────────────────────────────┐
│                     Memory System Flow                         │
└───────────────────────────────────────────────────────────────┘

  User Message → memoryHandler.getUserContext()
                       │
                       ↓
              ┌────────────────┐
              │  Try Redis     │  ← Fast (1-5ms)
              │  (24h cache)   │
              └────┬───────────┘
                   │
                   ├─ Hit  → Return cached context
                   │
                   └─ Miss → ┌────────────────┐
                             │  Try Postgres  │  ← Slower (10-50ms)
                             │  (persistent)  │
                             └────┬───────────┘
                                  │
                                  ├─ Found → Cache in Redis → Return
                                  │
                                  └─ Not Found → ┌────────────────┐
                                                 │  Try JSON File │  ← Fallback
                                                 │  (fallback)    │
                                                 └────┬───────────┘
                                                      │
                                                      └─ Return (or empty)

  Response Generated → memoryHandler.updateUserMemory()
                              │
                              ↓
                    ┌──────────────────┐
                    │  Write to Redis  │  ← Always (background)
                    │  TTL: 24 hours   │
                    └──────────────────┘
                              │
                              ↓
                    ┌──────────────────┐
                    │ Write to Postgres│  ← Always (background)
                    │  + emotion hist  │
                    └──────────────────┘
                              │
                              ↓ (if DB fails)
                    ┌──────────────────┐
                    │ Fallback to JSON │  ← Safety net
                    │  File storage    │
                    └──────────────────┘
```

## Deployment Topology

```
┌─────────────────────────────────────────────────────────┐
│                Production Deployment                     │
└─────────────────────────────────────────────────────────┘

                    ┌─────────────────┐
                    │   PM2 Process   │
                    │   Manager       │
                    │                 │
                    │  ┌───────────┐  │
                    │  │ Bot       │  │
                    │  │ Instance  │  │
                    │  │           │  │
                    │  │ Auto-     │  │
                    │  │ Restart   │  │
                    │  │           │  │
                    │  │ Memory    │  │
                    │  │ Limit:    │  │
                    │  │ 500MB     │  │
                    │  └─────┬─────┘  │
                    └────────┼────────┘
                             │
              ┌──────────────┼──────────────┐
              │              │              │
              ↓              ↓              ↓
    ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
    │   Redis     │  │  Postgres   │  │   Logs      │
    │   Cache     │  │  Database   │  │   Rotation  │
    │             │  │             │  │             │
    │  Port 6379  │  │  Port 5432  │  │  logs/      │
    │             │  │             │  │  error.log  │
    │  Health ✓   │  │  Health ✓   │  │  out.log    │
    └─────────────┘  └─────────────┘  └─────────────┘
          │                  │
          │                  │
          └──────────┬───────┘
                     │
                     ↓
          ┌──────────────────┐
          │  Redis Commander │
          │  Web UI          │
          │  Port 8081       │
          └──────────────────┘

Docker Compose manages: Redis, Postgres, Redis Commander
PM2 manages: Bot process
```

## Technology Stack Visualization

```
┌─────────────────────────────────────────────────────────────┐
│                    Application Stack                         │
└─────────────────────────────────────────────────────────────┘

Layer 1: Runtime & Process Management
├─ Node.js 20
├─ PM2 (Process Manager)
└─ Docker (Containerization)

Layer 2: WhatsApp Integration
├─ whatsapp-web.js
├─ Puppeteer
└─ QR Code Terminal

Layer 3: AI & Machine Learning
├─ LangChain.js
├─ OpenAI (GPT-4o-mini, Whisper, DALL-E)
├─ Google Gemini
└─ 🆕 Emotion Analysis (GPT-4o-mini)

Layer 4: Data Storage (🆕 Enhanced)
├─ Redis 7 (Cache Layer)
│   └─ ioredis client
├─ Postgres 16 (Persistence)
│   └─ pg client
└─ JSON Files (Fallback)

Layer 5: Application Logic
├─ 🆕 Handlers (emotion, memory, message)
├─ Services (media, voice, client, 🆕 openai, redis, pg)
├─ 🆕 Utils (smart delays, data extraction)
├─ AI Engines (v2.0 + legacy)
└─ Personas (social media, consultant)

Layer 6: Monitoring & Management
├─ PM2 Dashboard
├─ Redis Commander Web UI
├─ Log Files (error, out, combined)
└─ Health Checks
```

## Key Integration Points

```
Existing System          Enhanced Layer          Infrastructure
───────────────          ──────────────          ──────────────

commandDispatcher   ←──→ messageHandler    ←──→ Redis/Postgres
       │                      │                       │
       │                      ├──→ emotionHandler ──→ OpenAI
       │                      │
       │                      └──→ memoryHandler
       │
       ↓
conversationOrchestrator
       │
       └──→ conversationalEngine
                  │
                  └──→ LLM (existing)

                 smartDelay
                     ↓
              Response Timing
                     ↓
              User receives message
```

---

## Legend

- 🆕 = New component
- ✓ = Validated/tested
- ← → = Bidirectional communication
- → = Unidirectional flow
- ├─ = Component relationship
- └─ = Terminal component
